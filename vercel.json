{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/fuelfool build",
  "outputDirectory": "artifacts/fuelfool/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
